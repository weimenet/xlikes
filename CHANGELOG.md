# 更新记录

## v1.1.0

相对 GitHub 上的 v1.0.0（2026-08-26）的全部改动。

### 新增

- **内置抓取**：顶部下载栏粘贴链接即可抓取（可多条，按 `http` 自动切分，兼容新版分享链接格式），容器内调用 gallery-dl 按命名规则落盘并定向入库；队列弹窗显示进行中 / 排队中 / 失败 / 已完成，失败可重试，任务落盘保留 7 天
- **抓取时顺带入库文案**：抓媒体的同时把帖子正文、作者昵称与头像写进文案缓存，媒体目录里不留额外文件；已存在、被跳过下载的帖子同样能补上文案（走登录态，比公开接口稳）
- **抓取管理页**：状态统计（总数 / 已抓取 / 待抓取 / 抓取失败 / 原帖不存在）、按状态筛选、进度条与抓取中状态、单条重试 / 手填文案
- **gallery-dl 批量补抓**：`刷新` = 补抓还没有结论的帖子，`重试全部失败` = 重抓失败的帖子；都只取元数据、不下载媒体，并带风险控制（串行、每两条 2–5 秒随机间隔、每 30 条一次长停顿、连续 15 条无收获自动停止、单次上限 300 条、可手动停止）
- **缩略图加速**：320px WebP + 内容版本号长缓存（缩略图变了才刷新，不变一直命中缓存）；视频优先抓 X 封面，抓不到再本地抽帧
- **NSFW 模式**：顶部滑动开关，一键模糊全部媒体；视频在开启时提示「NSFW 模式无法播放」且点击无效；头像不参与模糊；默认开启，用户手动改过则记住
- **防盗链**：媒体 / 缩略图 / 头像校验 Referer 与 Origin，外站引用返回 403，无 Referer 的「另存为」正常放行
- **多设备会话**：同一账号多设备并存，可查看已登录设备并踢下线；修改密码使所有设备失效
- **登录设备页**：设备、IP、最后活跃时间，当前设备标记
- **添加时间排序**：新增 `addedAt` 字段，历史数据用文件修改时间补齐、新文件用首次发现时间
- **文案内 @ID 跳转**：帖子详情页正文里指向站内已有账号的 `@ID` 自动变成站内链接
- **用户页**：头部新增「X 主页」按钮，可直接跳转对应 X 用户主页
- **排序记忆**：排序选择按登录账号分别记住（刷新、重开页面都保留）
- **搜索按钮**：搜索框右侧新增「搜索」按钮，触屏没有回车键也能触发
- **日期筛选控件**：显示「起始日期 / 结束日期」，点击弹系统日期选择器，右侧 `×` 清空
- **排序图标**：排序从文字下拉改为 SVG 图标按钮组，风格统一、点一下即切换
- **重复抓取识别**：提交链接时先比对库内已有的帖子，已在库的直接标记为「已存在」、不重复下载（提交提示会说明有几条被跳过）；已存在的任务可以点「仍然抓取」强制入队，用于老帖当年没抓全、需要补图的情况
- **抓取记录可跳转**：队列弹窗与抓取页的任务行，链接可直接点击跳转；已完成 / 已在库的任务额外带一个「查看」按钮，直达该帖的站内详情页（队列弹窗点击后会先关闭弹窗）
- **gallery-dl 检查更新**：控制台抓取页标题右侧显示当前版本号，点「检查更新」会去 PyPI 比对最新版本——有新版按钮变绿「立即更新」，无新版显示暗灰「最新版本」；更新在容器内直接 pip 升级，升级后自动自检（`--simulate` / `--dump-json` 选项与 `postprocessor.metadata`、`extractor.twitter` 模块），任一项不通过会自动回滚到旧版本。仅超级用户可见、可操作，检测到有抓取任务在跑时会拒绝更新（pip 升级写在容器可写层，重建容器后会回到镜像版本）

### 改进

- **性能**：`/api/feed` 从约 16.5 秒降到约 90 毫秒（头像目录改为内存索引，图片 URL 与文案只对当前分页生成），用户列表接口从约 5.3 秒降到约 60 毫秒
- **卡片信息区重做**：头像 + 第一行「用户名 / @ID」+ 第二行「日期」+ 两行文案；瀑布流卡片、帖子详情页、ID 索引页统一同一套排布
- **全站圆角统一**：卡片 / 按钮 / 输入框 / 面板统一取「输入框高度的一半」为圆角，并随实际高度动态计算
- **控制台布局**：窄屏下内容区铺满（修掉半宽留白）；操作按钮移到进度条右侧；状态提示跟在标题后面；压缩进度条上下留白
- **响应式**：手机 / 平板 / 桌面与不同 DPI 自适应，顶部栏、下载栏、队列弹窗按宽度折行或限高滚动
- **菜单与页面**：侧栏「文案」更名为「抓取」，页面标题为「抓取管理」；「扫描」页仅对超级用户显示；普通账号在「抓取」页只看到自己提交的任务（含状态与重试），看不到全站文案管理
- **加载失败自动重试**：页面加载失败会自动重试两次（间隔 3 秒），仍失败才提示刷新
- **抓取配置带注释（TOML）**：配置文件改为 `gallery-dl.toml`，逐条注释三项关键设置——登录态路径、媒体库位置、入库命名规则（文件名与目录结构的占位符含义、改动后果），并写明 cookies 的两种获取方式（浏览器扩展导出 / 手动写入 `auth_token` 与 `ct0`）以及应当放到宿主机的哪个路径；调用参数同步改为 `--config-toml`（gallery-dl 的 `-c` 只按 JSON 解析，YAML 需要额外依赖，TOML 由 Python 内置支持）
- **详情页精简**：去掉帖子详情页底部的「原帖：<链接>」一整块（含其上方分割线），原帖入口仍保留在头部的「查看原文」按钮

### 修复

- **建号 / 改密被覆盖**：外部脚本写入的新账号或新密码，会被服务进程内存里的旧快照覆盖。现在落盘前先读磁盘并合并（新增账号直接吸收，已存在账号按 `updatedAt` 取较新版本），建号与改密都不再需要重启
- **媒体路径过滤**：拦截通过媒体路径访问隐藏目录（如数据目录）
- **UI 细节**：下载栏按钮被挤成两行、设备页按钮与列表贴在一起、文案页信息粘连等

### 安全

- **越权修复**：后台管理类接口（手动扫描、批量补抓的触发 / 停止 / 状态查询）补充权限校验，仅超级用户可用；抓取任务的重试增加归属检查，只能重试自己提交的任务
- **前端同步隐藏**：文案页的「刷新 / 重试全部失败」、扫描页的「立即扫描」对普通用户不渲染
- **提交者信息隔离**：文案列表里的「提交抓取的用户」与对应筛选仅超级用户可见，普通用户请求时服务端不返回名单、不接受筛选参数，并剥离每条记录里的提交者字段
- **抓取风控**：gallery-dl 批量抓取限速、自动止损，避免账号被平台限制
